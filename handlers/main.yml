---
# zerodowntime.bind9/handlers/main.yml


- name: 'restart bind'
  service:
    name: "{{ bind__service_name }}"
    state: restarted


- name: 'reload bind'
  service:
    name: "{{ bind__service_name }}"
    state: reloaded
